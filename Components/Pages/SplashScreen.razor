@* SplashScreen.razor *@
@* Rimuovi "@inject NavigationManager Navigation" se non lo usi in questo componente *@

<div id="splash-screen">
    <div class="splash-content">
        <img src="/images/plannerlogo.png" alt="PlannerApp Logo" class="splash-logo">
        <div style="width: 200px;">
            <MudProgressLinear Color="Color.Secondary" Indeterminate="true" Class="my-7" />
        </div>
    </div>
</div>

@code {
}

<style>
    /* I tuoi stili CSS sono corretti e possono rimanere qui */
    #splash-screen {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: #F5F6FA; /* Colore dominante */
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 9999;
        font-family: 'Roboto', sans-serif;
        transition: opacity 0.6s ease-out; /* Per la transizione fade-out */
    }

        #splash-screen.fade-out {
            opacity: 0;
            pointer-events: none; /* Rende il div non cliccabile quando scompare */
        }

    .splash-content {
        text-align: center;
    }

    .splash-logo {
        width: 120px;
        height: auto;
        margin-bottom: 30px;
    }
</style>
